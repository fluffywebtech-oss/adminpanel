# Supabase Configuration
# Copy this file to .env and fill in your Supabase project values
# Find these in your Supabase dashboard → Settings → API
VITE_SUPABASE_URL=https://ioblbfugnhtghvxbyeos.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# AI Listing Generator (server-side only — never exposed to the browser)
# Get a key at https://console.anthropic.com → API Keys
# Local dev: put it in .env (NOT .env.example) and restart `npm run dev`
# Production: set it in Vercel → Project Settings → Environment Variables
ANTHROPIC_API_KEY=your-anthropic-api-key-here
